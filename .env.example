# --- Server Configuration ---
PORT=3001

# --- Documenso API Configuration ---
# This is the URL your Node.js server uses to talk to the Documenso container.
DOCUMENSO_API_URL=http://localhost:3000

# Generate this from your Documenso instance under Settings -> API Keys.
DOCUMENSO_API_KEY=YOUR_DOCUMENSO_API_KEY_HERE

# --- MinIO (S3) Configuration ---
# This is the endpoint your Node.js server (on localhost) uses to connect to the MinIO container.
S3_ENDPOINT=http://localhost:9000

# This is the URL the Documenso CONTAINER uses to access the MinIO CONTAINER. It MUST use the service name.
MINIO_PUBLIC_URL=http://minio:9000

# These must match the values in your docker-compose.yml file.
S3_BUCKET=documenso
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_REGION=us-east-1

# --- React App Environment Variables (prefixed with VITE_) ---
# This URL is used by the frontend embed SDK to connect to the Documenso UI.
VITE_DOCUMENSO_URL=http://localhost:3000

